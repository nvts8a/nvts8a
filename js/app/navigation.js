
/*
function rotateSelected() {

	var selected = $('img#experience');

	var newXPosition = parseInt( selected.css('left') ) + 10;
	selected.css( 'left', newXPosition + 'px' );

	setTimeout( function() { rotateSelected() }, 20 );
}

rotateSelected();
*/
